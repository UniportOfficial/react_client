import 'swiper/css';
import Survey from '../../components/user/Survey';

export default function Register(){
    return (
        <main className="w-full">
            <Survey></Survey>
        </main>
    )
}